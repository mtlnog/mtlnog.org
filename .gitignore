node_modules/
dist/
.astro/
.DS_Store

# Local environment values (never commit real credentials)
.env
.env.*
!.env.example

# Local Node version manager config (use .nvmrc as the committed source of truth)
mise.toml
.mise.toml
